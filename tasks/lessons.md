# lessons.md — Errores y patrones aprendidos
<!-- Actualizar tras cada corrección del usuario -->
<!-- Nunca borrar entradas existentes — solo añadir -->
<!-- Formato nueva entrada: YYYY-MM-DD | módulo | descripción | regla -->

---

## REGLAS CRÍTICAS (errores documentados del proyecto)

### Excel y openpyxl
- **[CRÍTICO] Excel abierto al escribir** → fallo silencioso o corrupción de archivo
  → REGLA: Avisar SIEMPRE al usuario antes de cualquier escritura Excel. Sin excepción.
- **Sobreescritura sin leer primero** → pérdida de datos existentes
  → REGLA: save_to_excel() SIEMPRE lee hoja existente antes de escribir. Dedup por unique_col.
- **Backup omitido** → sin posibilidad de recuperación si algo falla
  → REGLA: Backup automático en datos/backups/ antes de la primera escritura de cada sesión

### Scripts batch (.bat)
- **ERRORLEVEL dentro de bloques if ()** → no se actualiza en Windows
  → REGLA: Usar goto + labels para manejo de errores. Nunca if ERRORLEVEL anidado.
- **PYTHONPATH no configurado en gmail_auto.bat** → `ModuleNotFoundError: nucleo` tras reorganización de imports
  → REGLA: Si un script usa módulos del proyecto (nucleo/, config/), el bat debe hacer `set "PYTHONPATH=%PROJECT_ROOT%"` antes de ejecutar Python. Verificar .bat tras cualquier cambio de imports.

### Gmail API
- **Token OAuth2 caducado** → error silencioso o crash en gmail.py
  → REGLA: Si gmail.py falla con error de autenticación → ejecutar gmail/renovar_token_business.py
- **MIME type con barra invertida** → text\html en vez de text/html
  → REGLA: Al parsear payloads Gmail API, verificar que el MIME type usa barra normal.
- **Cambio de scope OAuth no se aplica con solo editar SCOPES** → la librería usa los scopes persistidos dentro de `token.json`, no los del código. Síntoma: 403 "insufficient authentication scopes" aunque el código declare el scope nuevo.
  → REGLA: Al cambiar scope, borrar/renombrar `token.json` (`mv token.json token.json.bak.YYYYMMDD`), ejecutar `python gmail/renovar_token_business.py` (navegador OAuth), copiar el nuevo `token.json` al VPS con `scp`.
- **Serialización de credenciales filtra scopes → se pierden al refresh** → Pasar `scopes=[...]` a `Credentials.from_authorized_user_file(token, scopes=X)` hace que, al refrescar y volcar con `creds.to_json()`, se persista SOLO el subconjunto `X`, destruyendo los scopes realmente autorizados. Caso real (21/04/2026): el scope `drive` desapareció de `token.json` entre el 18/04 y el 20/04 porque cada run de gmail.py pasaba solo `[gmail.readonly, gmail.modify]` y reescribía el token.
  → REGLA: En `gmail/auth_manager.get_credentials()` cargar sin parámetro `scopes`; dejar que el token dicte qué está autorizado. Los callers (`get_gmail_service`, `get_drive_service`) tampoco deben pasar scopes.
  → **REGLA ALTERNATIVA** (cuando NO se puede evitar pasar scopes — caso `gmail/gmail.py:706` que `auth_manager` no controla): listar TODOS los scopes que el token tiene autorizados, no solo los del módulo concreto. La lista pasada a `from_authorized_user_file()` es lo que `creds.to_json()` persistirá tras refresh — si pones un subconjunto, persistes un subconjunto.
  → REVISITADO (04/05/2026): el mismo bug reapareció porque solo se parcheó `auth_manager.py` el 21/04, pero `gmail.py:706` seguía pasando `CONFIG.GMAIL_SCOPES = [gmail.readonly, gmail.modify]` (sin drive). Cada ejecución de gmail.py refrescaba y borraba `drive` del token. Síntoma: scope desaparece entre ejecuciones sin que nadie lo haya tocado. Detección: tras un fix de OAuth, `grep -rn "from_authorized_user_file" gmail/ scripts/ streamlit_app/` para asegurar que TODOS los call sites o pasan `None` o pasan la lista completa de scopes del proyecto.
  → RESUELTO (05/05/2026): parche en `gmail/gmail.py:191-194` añadiendo `drive` a `GMAIL_SCOPES`. Token regenerado con 4 scopes (gmail.readonly + gmail.modify + business.manage + drive). Drive API verificada (list carpeta canónica OK). Test robustez: `creds.to_json()` preserva los 4 scopes tras load. Deuda técnica: refactor de `gmail.py:conectar()` para delegar en `auth_manager.get_gmail_service()` y eliminar la duplicación permanentemente. Mientras existan dos call sites paralelos de `from_authorized_user_file`, **cualquier scope nuevo del proyecto debe añadirse a TODAS las listas** o el bug volverá.

### Formato de datos
- **Coma decimal en datos 2024** → "3,51" en vez de "3.51"
  → REGLA: Al leer Excel histórico 2024, convertir comas a puntos antes de operar.
  → REGLA: Datos 2025+ usan punto. No aplicar conversión si el archivo es de 2025+.
- **Formato moneda en Excel/email** → SIEMPRE usar formato europeo: X,xx € (coma decimal, símbolo al final)
  → REGLA: Al escribir importes en Excel o HTML, usar `f"{v:.2f}".replace(".", ",") + " €"`
  → REGLA: Fechas en columnas Excel de ventas: formato DD-MM-YY (no YYYY-MM-DD)

### Extractores PDF
- **Extractor nuevo sin manejo de excepciones** → factura sin procesar si falla
  → REGLA: Todo extractor debe devolver datos parciales ante fallo, nunca lanzar excepción.
- **OCR como método primario** → solo para PDFs imagen pura confirmados
  → REGLA: Usar OCR primario solo en extractores confirmados: JIMELUZ, CASA DEL DUQUE, LA LLILDIRIA.
  → Para el resto: pdfplumber o pypdf con fallback_ocr=True.
- **`extraer_numero_factura` eliminado en Parseo v5.18** → gmail.py debe usar `extraer_referencia`
  → REGLA: Al crear extractores nuevos, solo implementar `extraer_referencia` (nunca `extraer_numero_factura`).
- **MAESTRO con `ARCHIVO_EXTRACTOR=X.py` pero archivo inexistente** → gmail.py loguea `Extractor no encontrado` y cae al fallback genérico, dejando filas Excel zombi (TOTAL/REF vacíos). Caso real (04/05/2026): `ACEITES DE ESPECIALIDAD JALEO` con `ARCHIVO_EXTRACTOR=jaleo.py` configurado en MAESTRO desde hacía meses, pero `Parseo/extractores/jaleo.py` nunca se creó → 2 zombis acumuladas.
  → REGLA: Al dar de alta un proveedor en MAESTRO con `TIENE_EXTRACTOR=SI` y `ARCHIVO_EXTRACTOR=X.py`, crear el archivo el mismo día. Si se pospone, marcar `TIENE_EXTRACTOR=NO` hasta que esté listo, así gmail.py usa fallback genérico explícito (no roto).
  → REGLA: Para detectar futuros casos, ejecutar periódicamente: para cada fila MAESTRO con `TIENE_EXTRACTOR=SI`, verificar `Path(extractores_dir / archivo_extractor).exists()`. Discrepancia → alerta.
- **Mismo proveedor con 2 formatos PDF distintos** → un solo regex no cubre ambos casos. Caso real (04/05/2026): JALEO tiene formato "factura moderna" (cabecera one-line `Factura N\d+ Fecha dd/mm/yyyy ... Total X€`) y formato "presupuesto" (bloques separados con `NÚMERO DE ORDEN #\d+`, `FECHA DE ORDEN dd-mm-yyyy`, `TOTAL : X €`).
  → REGLA: Cuando un proveedor tiene >1 formato, escribir CADA método (`extraer_total/fecha/referencia`) con regex jerárquicos: primero el patrón más específico, luego el genérico. Validar smoke test con un PDF de cada formato. Documentar los formatos en el docstring del módulo.
  → REGLA: Cuidar colisiones con campos parecidos. Caso JALEO: `extraer_total` debe matchear `TOTAL : 523,50 €` pero NO `TOTAL PARCIAL : 698,00 €` → usar `^TOTAL\s*:` con `re.MULTILINE`. Y `extraer_referencia` debe priorizar `Factura N\d+` sobre `#\d+` porque PDF1 contiene `Observaciones #1859` que NO es la REF real.
- **PDFs con jerarquía visual cliente/proveedor invertida** → la heurística genérica de gmail.py registra el CLIENTE como proveedor. Caso real (04/05/2026): factura COMPROVINO con cabecera maquetada poniendo "COMESTIBLES BAREA" + "Elena de Miguel" + email cliente en posición visualmente prominente, mientras "COMPROVINO SL / BODEGABIERTA" aparecía a la izquierda con jerarquía menor. El CIF B85501989 es del proveedor pero acabó asociado al "proveedor nuevo" COMESTIBLES BAREA en el log.
  → REGLA: cuando el log diga `Proveedor NUEVO ... CIF X` y X coincida con un CIF cliente conocido (B87760575 para Tasca Barea SLL en este negocio), hay alta probabilidad de bug de confusión cliente/proveedor. Investigar el PDF original. Solución: extractor dedicado con `cif` estático (no extraer del PDF — los 2 CIFs suelen estar en la misma línea y separarlos sin contexto es frágil) + alta MAESTRO con alias múltiples (razón social + nombre comercial; ambos llegarán como remitente en distintos emails).
  → REGLA: smoke test del extractor debe incluir aserto defensivo: `assert extractor.cif == "<CIF_PROVEEDOR>"` y `assert extractor.cif != "<CIF_CLIENTE>"`. Si en el futuro alguien cambia el CIF estático por accidente o alguien añade `extraer_cif` que captura el CIF cliente, el smoke test lo pilla.

### Portes y envío en facturas
- **Portes como línea separada** → error de IVA y descuadre en totales
  → REGLA: Portes/envío SIEMPRE proporcionales entre productos.
  → FÓRMULA: (coste_envío × (1 + IVA_envío/100)) / (1 + IVA_productos/100)

### Tests obsoletos tras refactor de la API testeada
- **Tests CI fallan en collection con `AttributeError: module 'X' has no attribute 'Y'`** → la API que el test importa fue renombrada/eliminada en algún refactor anterior y los tests no se actualizaron. Síntoma típico: tests llevan tiempo rotos pero nadie los arregla porque están mezclados con deuda CI más amplia.
  → REGLA: cuando un test rompe en collection con `AttributeError`/`ImportError` sobre símbolos del código de producción, el primer paso es `git log -S "<simbolo>" -- <archivo>` para ver dónde se eliminó. Suele apuntar a un merge o refactor concreto. Ese commit decide si la feature fue descartada (→ borrar tests) o si fue regresión (→ restaurar).
  → REGLA: distinguir B (constante perdida) vs A (tests obsoletos) por uso en producción: `grep -rn "<simbolo>" --include="*.py" src/` (excluyendo tests). Si solo aparece en tests → tests obsoletos, borrar. Si aparece también en código de producción → regresión, restaurar.
  → REGLA: **al hacer merge con conflictos donde se prioriza un lado completamente** (caso 18/04/2026 cec9306 "prioridad código PC"), revisar la suite de tests antes de empujar — los tests del lado descartado se quedarán colgados. Es deuda que se acumula silenciosa hasta el siguiente push que dispara CI.
- **Errores en collection enmascaran otros errores**: cuando collection muere al primer fallo, los tests posteriores ni se intentan. Caso real (05/05/2026): tras borrar los 2 tests obsoletos por `TOTAL_MIN_SOSPECHOSO`, en CI aparecieron 4 ImportError NUEVOS de `google`, `numpy`, `dotenv`, `googleapiclient` (deps que `pip install -e ".[dev]"` no cubre porque el workflow las omite del extra-install). En local también afloraron 22 fallos por falta de `pytest-asyncio` que estaban ocultos. Total: 26 problemas preexistentes que llevaban semanas tapados detrás de 2 ImportError.
  → REGLA: tras arreglar un fallo de collection, ejecutar la suite COMPLETA y leer la salida tanto en local como en CI. No dar por bueno "los tests pasan" si CI sigue rojo por motivos distintos. Documentar como backlog separado, no expandir scope del fix actual.
  → REGLA: divergencia entre venv local y workflow CI suele ser fuente de sorpresas. Si el workflow tiene un `pip install` con extras manuales además de `pip install -e ".[dev]"`, esos extras son señal de que el extra `dev` está incompleto y se van añadiendo deps a mano. Refactor recomendado: consolidar TODAS las deps necesarias para tests en `pyproject.toml [project.optional-dependencies] dev`.
  → APLICADO (05/05/2026 noche-3): consolidación realizada. 11 deps movidas/añadidas a `[dev]` (google-api-python-client, google-auth, google-auth-oauthlib, numpy, openpyxl, pdfplumber, pydantic, python-dotenv, rapidfuzz, pandas, fastapi, python-multipart) + workflow reducido a `pip install -e ".[dev]"`. Suite local pasó de 114 passed + 22 fail a 136 passed + 0 fail. **CI requirió 3 iteraciones** para verde porque cada push descubría una dep transitiva más que el venv local sí tenía: `1de22b5` (consolidación) → `df71401` (+pandas+fastapi) → `ab94d04` (+python-multipart) → success 179 passed.
  → REGLA: tras consolidar deps en `[dev]`, antes de hacer push hacer dry-run mental de "¿qué tengo en mi venv que NO esté en el repo?" — `pip freeze | diff <(pip-extract-deps pyproject.toml [dev])` puede ayudar. La trampa repetida: deps transitivas que no son obvias del código (FastAPI necesita `python-multipart` en runtime para form data, pero no aparece en imports del test). Iterar reactivamente cada vez que CI revele una nueva es válido pero costoso (cada iteración = 1-2 min).

### Validar heurísticas en frío contra datos reales antes de aplicar
- **Cuando una auditoría propone heurísticas (regex, scoring, capas de prioridad), validarlas en frío contra datos reales antes de aplicar el fix completo**. Caso real (06/05/2026 Bloque B): la auditoría inicial proponía 3 capas para `_nombre_aproximado`. Al analizar 2 PDFs reales del backlog (Torres Import + Aquí Santoña), descubrimos que se necesitaba una 4ª capa (sufijo societario) y un bug ADICIONAL no contemplado (filtrado no-factura para catálogos comerciales).
  → REGLA: coste de validar en chat ~20 min. Coste de no validar: aplicar un fix incompleto y descubrirlo en producción tras 1 ciclo de gmail.py manual.
  → REGLA: cuando se descubren ≥2 problemas relacionados durante validación, evaluar si agruparlos en una sesión combinada (Bloque B + filtrado no-factura) o separarlas. La agrupación tiene sentido si el blast radius solapa (ambos tocaban `_procesar_pdf`).

### Tests con __init__ pesado en CI: usar __new__ para skipping
- **Tests que invocan clases con `__init__` que carga archivos del repo gitignored fallan en CI runner**. Caso real (06/05/2026): `_make_processor()` invocaba `GmailProcessor(modo_test=True)` que carga `MaestroProveedores(CONFIG.MAESTRO_PATH)`. MAESTRO está gitignored → existe en local pero NO en CI runner → `FileNotFoundError`.
  → REGLA: si los métodos testeados NO necesitan los atributos creados en `__init__`, usar `Clase.__new__(Clase)` para crear instancia sin disparar `__init__`. Solución mínima sin mocks ni fixtures complejos.
  → REGLA detección: tests pasan local, fallan CI con `FileNotFoundError` apuntando a archivo gitignored → casi siempre es `__init__` que carga ese archivo. Verificar antes de ir a fixtures elaboradas.

### Lectura ≠ escritura: paths divergentes en MAESTRO/datos compartidos
- **Cuando un consumidor (gmail.py) lee de una ruta y los scripts del repo escriben en otra, las altas se pierden silenciosamente**. Caso real (06/05/2026): gmail.py v1.21+ leía `G:\Mi unidad\Barea - Datos Compartidos\Maestro\MAESTRO_PROVEEDORES.xlsx` (Drive Desktop) en Windows; los scripts de alta (`_alta_*.py`, manual de proveedor) escribían en `c:\...\gestion-facturas\datos\MAESTRO_PROVEEDORES.xlsx`. Las altas COMPROVINO (05/05) y JALEO (sesión anterior) llegaron a `datos/` pero NO a Drive. La próxima factura COMPROVINO o JALEO habría producido "Proveedor NUEVO" repitiendo el bug del 04/05.
  → REGLA: cualquier dato compartido (MAESTRO, diccionarios, índices) tiene que tener UNA fuente de verdad y todos los productores y consumidores deben apuntar al mismo path. Patrón a vigilar: lectura desde Drive/Cloud + escritura desde scripts del repo en `datos/`.
  → REGLA detección: cualquier alta de proveedor que no aparezca en la siguiente ejecución del consumidor → bug de paths divergentes. Comprobar `grep -rn "MAESTRO_PROVEEDORES.xlsx" .` y verificar que TODOS los call sites resuelven al mismo archivo.
  → REGLA solución: con 1 usuario, unificar en repo (`datos/`). Con multi-usuario, unificar en Cloud Y corregir scripts de alta para que escriban ahí (no en repo). Migrar B→A o A→B es trivial (1 commit aislado) si se diseña bien la abstracción `resolver_<recurso>_path`. Caso 06/05 v1.24: simplificación a 80 LOC menos + decisión documentada en `outputs/fix_maestro_fuente_verdad_20260506.md`.

### Auditoría → Bloque A (quick wins agrupados)
- **Cuando un audit produce ≥3 quick wins de bajo riesgo** (cosméticos + defensivos + bug fix puntual), agruparlos en una sola sesión + un solo commit. Bumpear `VERSION` del módulo y documentar todos los cambios en el header docstring del archivo. Tests unitarios mínimos por cada QW evita regresiones futuras.
  → REGLA: prompt autosuficiente con sustituciones literales (no descargas) + `cp` defensivo del archivo antes de tocarlo + `ast.parse` tras cada bloque + suite completa antes de commitear. Patrón aplicado el 06/05/2026 al Bloque A de gmail.py: 5 QW + 8 tests + commit `9611fb3`, suite 136 → 144 sin regresiones.
  → REGLA: incluir defensas contra causas raíz documentadas (caso QW3: token con scopes incompletos era la causa del bug OAuth Drive 28-30/04 — la validación defensiva impide que reaparezca silenciosamente). Convertir incidentes pasados en asserts.

### Sync VPS — line endings CRLF vs LF
- **md5 de un archivo Python en PC (CRLF) difiere del md5 en VPS (LF)** — comportamiento normal de git con `core.autocrlf` en Windows. Caso real (06/05/2026): tras push de `gmail.py` v1.23, md5 PC `7798cc9e...` ≠ md5 VPS `24efb225...`. NO indica fallo de sync.
  → REGLA: para archivos de texto (`.py`, `.md`, `.yml`), validar sync VPS por contenido lógico (line count + grep de markers únicos), no por md5. Para binarios (`.xlsx`, `.pdf`), md5 sí debe coincidir porque git no los toca.

### Pre-checks de ejecución manual gmail.py
- **Lock files también en `outputs/`** — gmail.py escribe a `outputs/PAGOS_Gmail_<periodo>.xlsx` (copia local del repo, además de Drive y Dropbox). Si Excel deja un lock zombi tras un crash en `outputs/~$PAGOS_Gmail_<periodo>.xlsx`, gmail.py aborta con `❌ ARCHIVO BLOQUEADO`. Caso real (05/05/2026 23:52): primer intento falló por `outputs/~$PAGOS_Gmail_2T26.xlsx` con mtime de 4h antes (Jaime ya había cerrado, lock zombi). Borrado con `rm` y relanzado OK.
  → REGLA: pre-checks de lock files DEBEN incluir `outputs/` además de `G:\Drive` y `Dropbox`. Las 3 ubicaciones tienen impacto distinto: locks en Drive/Dropbox bloquean SOLO el sync final; locks en `outputs/` bloquean la ejecución entera (gmail.py no puede ni hacer backup).
  → REGLA: si aparece lock con mtime >1h sin proceso Excel activo → es zombi tras crash, se puede borrar con `rm`.
- **Comando manual requiere `PYTHONPATH=.`** — `gmail/gmail.py` importa `nucleo.*` y `core.*`. Sin `PYTHONPATH=.` desde la raíz del repo, falla con `ModuleNotFoundError: No module named 'nucleo'`. La regla ya estaba documentada para `.bat`/`gmail_auto.bat`, pero el comando manual ad-hoc también la requiere. `docs/FLUJO_MANUAL_GMAIL.md` se corrigió 05/05 noche-4 para reflejarlo.
  → REGLA: cualquier doc que muestre el comando de gmail.py debe incluir `PYTHONPATH=.` y la ruta `gmail/gmail.py` (no `gmail.py`). Verificar con un test real, no copiar/pegar de memoria.

### Automatización vs ejecución manual
- **Cron silencioso para flujos que requieren validación humana** → problemas que se acumulan días/semanas sin que nadie los vea. Caso real (04/05/2026): la factura JALEO 27/04 quedó zombi 7 días hasta que se notó al revisar el log de una ejecución manual. El extractor faltaba en MAESTRO `ARCHIVO_EXTRACTOR=jaleo.py` desde hacía meses; un cron nocturno lo habría seguido fallando indefinidamente.
  → REGLA: Para flujos donde el log requiere lectura humana caso por caso (zombis, extractores nuevos, formatos PDF cambiantes, fallos OAuth), preferir ejecución manual con observación en directo al cron silencioso. Decisión 05/05/2026: `gmail.py` pasa a manual desde PC con Claude Code; `script_barea.py` (ventas, lunes) y backups siguen automáticos porque su flujo es mecánico.
  → REGLA: Si se reactiva un cron de un flujo "humano", plantar también alerta automática (email/Slack) cuando aparezca `[DRIVE FALLO]`, `Extractor no encontrado` o `ALERTA ROJA` en el log. Sin alerta, el cron es peor que la ejecución manual.
- **Premisas no verificadas sobre infraestructura** → afirmaciones que se propagan entre sesiones. Caso real (05/05/2026): registré en cierre 04/05 y 05/05 mañana que "el cron del viernes procesará..." sin haber consultado `crontab -l` ni una vez. Auditoría 05/05 tarde reveló que el cron NO existía — solo había un header huérfano `# Gmail facturas - viernes 03:00` que nunca se llegó a activar. La premisa contaminó dos sesiones.
  → REGLA: Antes de afirmar que un cron / timer / hook automático "procesará X", verificar con el comando concreto (`crontab -l`, `systemctl list-timers`, `ls /etc/cron.d/`). Si no se ha verificado, escribir "(suposición — verificar)" o no afirmarlo.

### Python imports — `ModuleNotFoundError` vs `ImportError`
- **`from package import submodule` con submódulo ausente NO lanza `ModuleNotFoundError`** → lanza `ImportError: cannot import name 'X' from 'Y'`. Solo `import X.Y` lanza `ModuleNotFoundError`. El segundo hereda del primero, **NO al revés**. `except ModuleNotFoundError` deja escapar el `from..import` caso.
  → REGLA: en código que tolera módulos opcionales (loaders, plugins, fallbacks), SIEMPRE `except ImportError`, nunca `except ModuleNotFoundError` aislado. Caso real (24/04/2026): `config/loader.py:_from_legacy()` capturaba solo `ModuleNotFoundError`; Streamlit Cloud lanzaba `ImportError` al hacer `from config import datos_sensibles`. Fix: cambiar a `except ImportError` (commit `5530c10`).

### Streamlit Cloud — deploy y requirements
- **`requirements.txt` canónico en Cloud es el JUNTO AL main file**, no el de la raíz → Streamlit Cloud resuelve `requirements.txt` adyacente a `streamlit_app/app.py`. Libs añadidas SOLO al de la raíz NO llegan a Cloud.
  → REGLA: al añadir libs que Streamlit use, editar `streamlit_app/requirements.txt` (o ambos si se quiere mantener paridad con dev local/VPS). Ver SPEC §13.10 para deuda de consolidación.
- **Warning "More than one requirements file detected" en logs de Cloud NO es inofensivo** → indica que Cloud está eligiendo uno distinto al esperado. Leer el log con cuidado.
- **Logging visible en UI ahorra iteraciones de debug** → patrón `try/except ImportError` con `st.error(...)` + `st.expander("Traceback")` + `st.code(traceback.format_exc())`. Caso real: el 4º bug de la cadena `/documentos` v2 se resolvió en 1 minuto gracias a este logging (el traceback literal lo envió el usuario).
  → REGLA: páginas Streamlit con imports de dependencias externas (Drive, APIs, etc.) deben envolver con este patrón al menos en la primera release.
- **Cachés bytecode `.pyc` agresivos en Cloud** → a veces un redeploy normal no los limpia. Si un fix no aplica tras push, probar commit vacío o reboot manual. Validar visualmente antes de asumir que funciona.

### Python path en apps multipágina Streamlit
- **`sys.path` centralizado en main file** → cada página hacía su propio `sys.path.insert(0, ROOT)`, frágil ante páginas nuevas. Centralizar en `streamlit_app/app.py` garantiza que cualquier `pages/<new>.py` hereda el path correcto.
  → REGLA: si las páginas importan módulos de la raíz del repo (nucleo, config, gmail, etc.), poner el `sys.path.insert(0, ROOT)` en el main file una sola vez.

### Config sensible — cascada de fuentes
- **Tres entornos, tres fuentes: Cloud/VPS/PC**. Usar `config/loader.py` con cascada `st.secrets → env var → datos_sensibles.py → default`. Importar directamente `from config.datos_sensibles import X` está deprecated (rompe bootstrap en Cloud).
  → REGLA: claves sensibles nuevas (CIFs, API keys, tokens) se leen via `config.loader.get("CLAVE", default)`. Config devs locales via `datos_sensibles.py` (gitignored). Cloud via `st.secrets`. VPS via env vars.

### Sincronización de documentación
- **Versiones desincronizadas entre archivos**
  → REGLA: Al modificar un módulo, actualizar versión en 2 sitios: header del código + tabla en CLAUDE.md.
  → Actualizar ESQUEMA al cerrar sesión si hubo cambios significativos.

### Filas zombi en PAGOS_Gmail
- **Concepto: "fila zombi"** = entrada en `PAGOS_Gmail_<periodo>.xlsx` que quedó con TOTAL vacío y/o "ALERTA ROJA" en OBS porque la versión de gmail.py que la insertó tenía bugs hoy corregidos. Los extractores actuales sí parsean correctamente esos PDFs. Las filas no se reescriben automáticamente — quedan petrificadas hasta que algo las reprocese. Detectado en `PAGOS_Gmail_2T26.xlsx` (28/04/2026, 6 filas tras versiones pre-v1.14 de gmail.py).
  → REGLA: tras cualquier upgrade significativo de gmail.py o de extractores, considerar pase de `scripts/resucitar_zombis.py` sobre los Excel del trimestre activo. El script es seguro (dry-run + backup + interactivo).
  → REGLA: NO sobreescribir PROVEEDOR/CIF/IBAN si el Excel ya tiene valor del MAESTRO — la fuente canónica es MAESTRO, no el extractor (atributo `nombre`/`cif`/`iban` del extractor puede ser una versión corta, p.ej. `'CERES'` vs `'CERES CERVEZA SL'`). Limitar la sobreescritura a TOTAL/REF/FECHA/FORMA_PAGO/CUENTA. Las discrepancias en columnas protegidas se logean WARN no bloqueante (pueden indicar datos del proveedor desactualizados o factura mal asociada).

### Bug nombrado de archivo en facturas multi-albarán
- **gmail.py guarda el PDF con la fecha del PRIMER ALBARÁN, no la fecha de factura** → en facturas que agrupan varios albaranes (típico ForPlan/MIGUEZ CAL), el nombre de archivo refleja la fecha del primer albarán y desincroniza con la fecha real de factura. Caso real (28/04/2026): factura 31/12/25 archivada como `ATRASADA 4T25 1205 MIGUEZ CAL SL TF.pdf` (12/05 = primer albarán), no como `1231`. Después alguien renombra el PDF en disco al nombre correcto (1231) pero la columna ARCHIVO del Excel sigue con 1205 → buscar el PDF por nombre exacto falla.
  → REGLA: Al investigar incidencias en proveedores con facturas multi-albarán, comparar `ARCHIVO` del Excel con el nombre real del PDF en disco. Si difieren en la fecha, sospechar bug de nombrado en gmail.py.
  → REGLA: La FECHA_FACTURA extraída del PDF también puede ser la del primer albarán. El extractor de MIGUEZ tiene este bug (28/04/2026, anotado en backlog). Verificar manualmente facturas multi-albarán hasta arreglar el extractor.

### Bug FORMA_PAGO en flujo gmail.py
- **gmail.py prefiere MAESTRO sobre PDF para FORMA_PAGO cuando el extractor no la extrae** → si el extractor no expone `extraer_forma_pago()` o equivalente, el flujo pone en el Excel lo que diga MAESTRO_PROVEEDORES, ignorando lo que esté en el PDF. Caso real: DEBORA GARCIA TOLEDANO factura 14/04/26 — PDF dice "Efectivo", MAESTRO tiene `TJ` → escribió `TJ` en Excel.
  → REGLA: Cuando un proveedor cambie ocasionalmente de forma de pago, esto causará un dato silenciosamente erróneo. Mientras el bug existe, revisar manualmente FORMA_PAGO de proveedores que paguen en efectivo o con métodos no fijos.
  → SOLUCIÓN PENDIENTE: o (a) que el extractor extraiga FORMA_PAGO del PDF; o (b) que el flujo prefiera SIEMPRE el PDF cuando esté presente.

### Patrón dual A/B de extractores
- **Dos firmas conviviendo**: la mayoría de extractores definen solo hooks individuales (`extraer_lineas/total/fecha/referencia`) que reciben TEXTO. Solo unos pocos (CERES y similares) definen `extraer(pdf_path)` que devuelve dict con todo. gmail.py:2376-2453 maneja ambos: si la instancia tiene método `extraer`, lo invoca; si no, abre el PDF con pdfplumber+OCR y llama a los hooks.
  → REGLA: Cualquier herramienta que necesite invocar extractores fuera de gmail.py (p.ej. `scripts/resucitar_zombis.py`) debe replicar este patrón dual para mantener paridad. La extracción de texto SÍ está expuesta como utilidad reutilizable: `nucleo.pdf.extraer_texto_pdf(ruta, metodo='pdfplumber', fallback=True)`. La selección de patrón A vs B sigue inline en gmail.py — duplicar con comentario apuntando a la línea origen.

### Identificadores persistentes vs únicos en `extraer_referencia`
- **Trampa**: Un campo en la factura puede llamarse `Invoice number` o `Customer ID` o `Reference` y ser **persistente del cliente/billing**, no único por factura. Si un extractor lo captura como REF, el anti-dup CIF+REF (gmail.py:2247-2255) descartará silenciosamente todas las facturas siguientes del mismo proveedor. Caso real (29/04/2026): `Parseo/extractores/anthropic.py:75-80` capturaba `Invoice number EXB4HCQN` (persistente) en lugar del `Receipt #2880-2984-9190` único → factura Anthropic 20/04 descartada como duplicada, situación que se repetiría cada mes.
  → REGLA: Al crear/revisar un extractor, comprobar con AL MENOS DOS facturas distintas del mismo proveedor que `extraer_referencia()` devuelva valores DIFERENTES. Si devuelve el mismo string para ambas, el patrón captura un identificador persistente — corregir.
  → REGLA: Para proveedores con CIF vacío (extranjeros tipo ANTHROPIC), el riesgo es mayor porque `factura_existe` cae al fallback `nombre|REF`, y el `nombre` también es constante → la unicidad depende ENTERAMENTE del REF. Doble cuidado.
  → DETECCIÓN: revisar periódicamente `datos/emails_procesados.json` por proveedores con N entradas en `emails` pero pocas/una entrada en `facturas` (clave repetida). Es señal de REF colisionando.

### Anclar regex de fechas al contexto correcto
- **Cuando un PDF tiene múltiples fechas en distintas secciones** (cabecera de factura + tabla de albaranes/líneas/vencimientos), el regex de extracción debe anclarse al contexto de la fecha de cabecera, no capturar la primera que encuentre. Caso de referencia: facturas MIGUEZ CAL multi-albarán contienen `31/12/25 A 4724 ...` en cabecera y `ALBARÁN A-3184 FECHA 05/12/2025` en tabla — un regex genérico capturaría la del primer albarán como fecha de factura.
  → REGLA: patterns útiles para anclar — precedido de `Factura`, `Fecha factura`, `F.Emisión`; o restringido a las primeras N líneas; o anclado a un patrón único de la cabecera (ej. `^DD/MM/YY\s+A\s+\d+` para identificar la línea inmediatamente debajo de "FECHA FACTURA CLIENTE N.I.F TELÉFONO PÁG.").
  → DETECCIÓN tardía (red de seguridad observacional, gmail.py:_check_fecha_vs_email): si `factura.fecha < internalDate − 7 días` en facturas recientes, el log emite WARNING con la sospecha de multi-albarán. NO bloquea; sólo invita a revisar el nombre del archivo manualmente.

### Bug irreproducible — no tocar
- **Si un bug histórico documentado no es reproducible con el código actual** (los tests pasan verde y no hay diferencia git ni md5 vs producción), NO modificar el código preventivamente. Documentar el caso histórico, añadir red de seguridad observacional (logs, alertas), y esperar a que el bug reaparezca para diagnosticarlo con datos. Modificar regex "por si acaso" cuando los tests pasan solo introduce riesgo nuevo sin eliminar el viejo.
  → CASO DE REFERENCIA: MIGUEZ multi-albarán abril 2026 — fila Excel archivada con fecha del primer albarán (`05/12`) en lugar de cabecera (`31/12`). Detectado al resucitar zombis el 28/04. Investigación 29-30/04 confirmó: extractor pasa smoke test verde sobre 4 PDFs reales (incluido el problemático), md5 PC == VPS desde marzo. Hipótesis no verificable: `.pyc` cached o pdfplumber distinto en la corrida del 18/04. ACCIÓN TOMADA: check defensivo en gmail.py (`_check_fecha_vs_email`), NO modificar extractor.
  → REGLA HEURÍSTICA: si gastas más tiempo "robusteciendo" un código que pasa los tests que diagnosticando el bug, estás haciendo cargo culture engineering. Para. Documenta. Espera datos.

### Bytes NUL en pdfplumber
- **pdfplumber a veces inserta `\x00` (byte NUL, ord=0) como separador en el texto extraído**. Caso real (29/04/2026): texto de `Invoice number EXB4HCQN 0006` en PDFs Anthropic — el "espacio" entre `EXB4HCQN` y `0006` resultó ser `\x00`, no un espacio real. Un regex con `\s*` falla porque `\s` cubre `[\t\n\r\f\v ]` pero NO el byte NUL.
  → REGLA: para campos críticos extraídos del PDF con pdfplumber, los regex deben usar `[\s\x00]*` cuando se permitan separadores. Alternativa: pre-limpiar el texto con `texto.replace('\x00', ' ')` antes de aplicar regex.
  → DETECCIÓN: si un regex que parece correcto cae al fallback inesperadamente, inspeccionar bytes con `for i, c in enumerate(texto[idx:idx+30]): print(i, repr(c), ord(c))` para ver el separador real.
  → ÁMBITO: cualquier extractor que reciba texto de pdfplumber. Casos especialmente sospechosos: PDFs generados por servicios USA (Anthropic, etc.) donde la tipografía y el embedding de texto pueden diferir del estándar europeo.

### Rescate manual de facturas duplicadas zombis
- **Cuando una factura se descarta como duplicado CIF+REF por bug del extractor**, el flujo correcto NO es reprocesar vía gmail.py (eso re-etiquetaría email + intentaría duplicar todo). El flujo correcto es:
  1. Parchear el extractor.
  2. Smoke test contra PDFs históricos del proveedor para confirmar que extrae REFs únicas.
  3. Re-parsear el PDF original con el extractor parcheado.
  4. Resucitar manualmente: subir PDF a Dropbox + añadir 1 fila a cada Excel canónico (PAGOS Drive, Provisional Drive, Provisional Dropbox).
  5. Limpiar el control DB del VPS (`emails_procesados.json`):
     - Borrar la clave zombi de `facturas`.
     - Insertar la clave nueva única.
     - Reemplazar la entrada de `emails[email_id]` (de `motivo: "Duplicado..."` al formato canónico de 5 campos: `message_id, fecha_proceso, proveedor, archivo, dropbox`).
     - NO tocar `ultima_ejecucion`.
  6. Deploy del extractor parcheado al VPS (scp + verificar md5 match).
  → REGLA: NO re-etiquetar el email Gmail con label `FACTURAS` para que el cron lo reprocese — eso intentaría duplicar el PDF en Dropbox (saltaría por hash dup pero contaminaría logs) y crearía una segunda fila Excel con la nueva REF (real) MIENTRAS la fila original errónea seguiría sin escribirse. El email permanece en `FACTURAS_PROCESADAS`.
  → Caso real (29/04/2026): factura Anthropic 20/04 (REF correcta `EXB4HCQN-0007`, total 90€) se rescató con script one-shot `scripts/_rescate_anthropic_20260420.py`. Backups del control DB pre/post conservados en `outputs/backups/` como evidencia.

### Diagnóstico de facturas "perdidas" — alias coloquial vs canónico
- **Cuando el usuario dice "la factura de X no se procesó"**, antes de buscar bug confirmar que X es exactamente el `nombre` del MAESTRO o uno de sus `alias`. Caso real (29/04): el usuario enumeró "Pili Blanco" y "Welldone" como no procesadas — ambas SÍ estaban procesadas, pero bajo el nombre canónico `BLANCO GUTIERREZ PILAR` (alias incluye `PILAR BLANCO`) y `DEL RIO LAMEYER RODOLFO` (alias incluye `WELLDONE`). El log y `gmail_resumen.json` listan SIEMPRE el nombre canónico del MAESTRO.
  → REGLA: para diagnosticar "no procesada", la cadena de verificación es:
    1. Email existe en Gmail (consulta API por `from:` o `subject:` libre).
    2. Label actual: `FACTURAS` (pendiente) vs `FACTURAS_PROCESADAS` (Label_5 — procesado).
    3. Email_id en `emails_procesados.json:emails` (con `proveedor` y `motivo`).
    4. Si `proveedor` aparece, es el nombre **canónico** del MAESTRO — buscar por ese nombre en log/Excel, no por el alias coloquial.
  → REGLA: el sistema procesa EL CONTENIDO de un email, no su fecha. Un email del 21/04 puede contener una factura del 13/11/2025 — el `archivo` reflejará la fecha real de la factura, no la del email.

### Pandas y tipos de datos
- **`df["col"].dtype == object` es frágil entre pandas 2.x y 3.x** → pandas 3.x reporta columnas de strings como dtype `str`, no `object`, rompiendo ramas condicionales que dependen de esa igualdad.
  → REGLA: Usar `pd.api.types.is_numeric_dtype(df["col"])` o `pd.api.types.is_object_dtype(...)` en lugar de comparar `.dtype == object`. Invertir la condición: rama fácil (es numérico) → usar directo; rama costosa (cualquier no-numérico, incluido string) → limpiar + `pd.to_numeric(errors="coerce")`.
  → REGLA: En limpiezas sobre strings de moneda española, eliminar también `\u00a0` (non-breaking space) y pasar `regex=False` a `.str.replace()` por claridad y velocidad.
- **VPS con versiones de paquetes divergentes de `requirements.txt`** → fallos reproducibles solo en producción (caso WC dashboard 20/04/2026: pandas 3.0.2 en VPS, 2.3.0 en PC).
  → REGLA: Mantener `requirements.txt` pinneado y exigir al VPS respetar los pins; instalar SIEMPRE con `pip install -r requirements.txt` tras cualquier upgrade.
  → REGLA: Tras detectar drift, alinear con `pip install "paquete==X.Y.Z"` y confirmar `pip show paquete` idéntico en ambos entornos.

### TODO pendientes (para SPEC v4.5)
- **Refactor `cargar_historico_wc.py`** → la línea 89 (`total_eur = f"{float(...):.2f}".replace(".", ",") + " €"`) escribe strings en el Excel. Esto fuerza que el dashboard tenga que limpiar "€" y coma en cada lectura. Decisión pendiente: escribir floats nativos y formatear solo al mostrar.

---

## REGISTRO DE CORRECCIONES
<!-- Claude Code añade aquí cada vez que el usuario corrija un comportamiento -->

| Fecha | Módulo | Error cometido | Regla añadida |
|-------|--------|----------------|---------------|
| 2026-03-13 | General | — | Archivo creado con errores documentados del proyecto |
| 2026-04-21 | ventas_semana/generar_dashboard | `if df["total"].dtype == object` saltaba el cleanup en VPS (pandas 3.x, dtype `str`) → `sum()` concatenaba strings y `float()` petaba | Usar `pd.api.types.is_numeric_dtype`; pinnear pandas 2.3.0 en VPS; añadidas reglas "Pandas y tipos de datos" |
| 2026-04-21 | gmail/auth_manager | `get_credentials(scopes=[...])` filtraba el objeto Credentials; al refrescar, `creds.to_json()` volcaba el subconjunto y sobrescribía token.json perdiendo el scope `drive` | No pasar `scopes` a `from_authorized_user_file()`; el token dicta los scopes autorizados. Regla nueva en sección "Gmail API" |
| 2026-04-24 | config/loader | `except ModuleNotFoundError` capturaba bien `import config.datos_sensibles` pero dejaba escapar `from config import datos_sensibles` (distinto `ImportError`) → /documentos caía en Streamlit Cloud | Usar `except ImportError` (cubre ambos casos porque `ModuleNotFoundError` hereda de `ImportError`). Regla nueva "Python imports" |
| 2026-04-24 | streamlit_app | Libs añadidas a `requirements.txt` raíz no llegaban a Cloud (Cloud usa el adyacente al main file) → imports fallaban en /documentos aunque localmente OK | Editar `streamlit_app/requirements.txt` al añadir libs para Cloud. Regla nueva "Streamlit Cloud — deploy y requirements". Deuda técnica documentada SPEC §13.10 |
| 2026-04-24 | streamlit_app | Iteraciones ciegas de debug en Cloud tras cada push — sin traceback visible costaba 1 round trip por bug | Patrón logging visible en UI (try/except + st.error + expander con traceback.format_exc). Adoptado en documentos.py, extensible |
| 2026-04-28 | scripts/resucitar_zombis | 1ª pasada del script sobreescribía PROVEEDOR (`CERES CERVEZA SL`→`CERES`) e IBAN (formato distinto) tomando del extractor en lugar del MAESTRO → habría roto la canonicalización del MAESTRO en filas resucitadas | Lista `COLUMNAS_PROTEGIDAS_MAESTRO=(PROVEEDOR,CIF,IBAN)`: si el Excel ya tiene valor, no sobreescribir y logear WARN. Regla nueva en sección "Filas zombi" |
| 2026-04-28 | scripts/resucitar_zombis | 1ª pasada marcaba FECHA_FACTURA como cambio aunque solo difería el formato (DD/MM/YY vs DD/MM/YYYY) | Normalizar siempre FECHA_FACTURA a DD/MM/YY (estándar `.claude/rules/excel.md`); comparar fechas por valor, no por string |
| 2026-04-28 | gmail/gmail.py | Bug nombrado multi-albarán descubierto: PDF de MIGUEZ CAL (factura 31/12/25) archivado en Excel como `1205` (1ª fecha de albarán) | Regla nueva en lessons "Bug nombrado de archivo en facturas multi-albarán" + entrada en backlog para revisar lógica de nombrado |
| 2026-03-13 | Parseo | ESQUEMA buscado en carpeta equivocada | ESQUEMA está en gestion-facturas/docs/, no en Parseo/ |
| 2026-03-13 | Gmail | REF "86" de BERNAL rechazada por gmail.py | gmail.py exigía len>=3, extractor genérico len>=2. Alineado a >=2 |
| 2026-03-13 | La Llildiria | Total 93.94 en vez de 172.75 | PyPDF no captura tabla totales en PDFs imagen. Añadido cálculo desde subtotales + cambio a OCR primario |
| 2026-03-27 | Gmail/bat | gmail_auto.bat falló con ModuleNotFoundError: nucleo | Añadir `set "PYTHONPATH=%PROJECT_ROOT%"` en bat. Verificar bat tras cambios de imports |
| 2026-03-30 | Ventas/bat | barea_auto.bat falló con mismo error que gmail_auto.bat | Verificar TODOS los .bat del proyecto tras cambios de imports, no solo el afectado |
| 2026-04-09 | Dashboard | Template Comestibles tenía datos JSON hardcodeados, sin placeholders | Verificar que templates HTML tengan placeholders `{{D_DATA}}` etc. Si no existen, generar_html no actualiza datos |
| 2026-04-09 | Dashboard | exportar_json_streamlit usaba DIAS de Comestibles para JSON de Tasca | Cada tienda debe tener su propio cálculo de DIAS. `calcular_DIAS` parametrizado con year_list |
| 2026-04-09 | Streamlit | Delta interanual incluía mes parcial → inflaba cifra año anterior | Usar `meses_completos` (no `meses_act`) para comparativas interanuales |
| 2026-03-30 | Ventas | `_to_float` no definido en email semanal | Tras refactorizar imports a nucleo.utils, verificar que TODAS las funciones referenciadas se importan |
| 2026-03-30 | Alerta | alerta_fallo.py pedía scope gmail.send no presente en token | Usar scopes del token existente (gmail.modify cubre envío). No inventar scopes nuevos |
| 2026-03-31 | WooCommerce | Evento creado como `simple` no aparecía en calendario web | Crear siempre como `type: "ticket-event"`, con categorías (Comestibles Barea + tipo) y tag Producto_destacado |
| 2026-03-31 | WooCommerce | Evento sin meta_data no aparecía en calendario web | Siempre incluir `_start_date_picker`, `_end_date_picker` en meta_data. Sin ellos el plugin de eventos no posiciona el evento |
| 2026-03-31 | WooCommerce | CERRADO con catalog_visibility hidden no aparecía en web | Los CERRADOS deben ser `visibility: visible` (como los manuales). stock=0 impide la compra |
| 2026-05-09 | gmail/gmail.py | Flujo "mover etiqueta antes de procesar" como red de seguridad creó bug crítico de email huérfano: si Excel falla mid-flight, el email queda sin etiqueta (no aparece en query pendientes) y sin entrada JSON (no se sabe que falló) → 29 emails históricos perdidos silenciosamente entre 20/03 y 10/04/2026 | REGLA: si tienes N pasos y el primero es destructivo (cambio de estado externo: mover etiqueta Gmail, borrar archivo, mover a /procesados), ese paso debe ir AL FINAL como confirmatorio, no AL PRINCIPIO como "red de seguridad", **siempre que exista otra red real** (capa hash MD5 en este caso). El argumento "lo movemos antes para no reprocesar si crashea" se cae si el sistema de duplicados ya cubre reprocesos |
| 2026-05-09 | Parseo/main.py | El "modo aislado" para diagnóstico (`-o C:\temp\... + --no-aprendizaje + --no-subcarpetas`) NO era aislado: subió a Drive `Compras/Año en curso/test_2T26_diagnostico.xlsx` y escribió `outputs/log_<ts>.txt` ignorando los flags. Resultado: Drive contaminado en sesión read-only de diagnóstico | REGLA: antes de declarar un modo "aislado" para tests/diagnóstico, auditar TODOS los side-effects de filesystem y red en el orquestador (no solo los obvios). Side-effects no listados explícitamente requieren un `--dry-run` real con flag explícito que envuelva CADA bloque de write/upload/sync. Validar con tests subprocess end-to-end, no asumir |
| 2026-05-09 | tests CLI con side-effects | Tests del flag `--dry-run` con mocks de helpers internos requería refactor del orquestador y era frágil (tests pasaban con bug regresado si el formato de log cambiaba) | REGLA: para validar comportamiento end-to-end de un script con side-effects (Drive uploads, file writes), **subprocess + captura stdout/stderr es más robusto que mocks**. Fixture `scope="module"` que ejecuta el run UNA vez y los N tests inspeccionan distintas partes del output. Aplicado en `Parseo/tests/test_dry_run.py` (8 tests, ~5s total). Documentar fragilidad de checks por substring (`[DRIVE OK]`) en docstring del test para que un futuro rename del log lo detecte |
| 2026-05-09 | metodología | Prompt v1.26 lock-safe asumía paths inexistentes (`compras/gmail.py` en lugar de `gmail/gmail.py`), versiones equivocadas (v5.18 vs v5.20 real), flag inexistente (`--dry-run`), y conteo aproximado (24 vs 29 reales). Sin auditoría preventiva, habríamos implementado contra una realidad imaginada | REGLA: cuando un prompt detalla file paths, versiones, flags o conteos exactos, ANTES de implementar verificar contra el código real (Read + Grep). El prompt de la skill `gestion-facturas` lo pide explícitamente ("Si algo de lo descrito en este prompt no coincide con el código real, PARAR y reportar la discrepancia antes de implementar"). En esta sesión, la auditoría detectó 8 discrepancias antes de tocar código |
| 2026-05-11 | git/github | `closes #N` en el commit message `5d9a2e0` cerró automáticamente el issue #5 del repo Parseo al hacer push. Pero el contenido del commit (unificar VERSION en main.py) no tenía nada que ver con el issue real #5 (bug cp1252 `UnicodeEncodeError` con `─`). Daño: issue mal cerrado, reapertura manual + comentario aclaratorio | REGLA: antes de usar `closes #N` / `fixes #N` / `resolves #N` en un commit message, verificar el título del issue con `gh issue view N -R <owner>/<repo>`. Si la temática del issue no coincide al 100% con el cambio del commit, NO usar el keyword. Mejor poner `(see #N)` o no referenciar el número. Razón: el keyword es irreversible una vez hecho push — GitHub registra el cierre en el timeline y la referencia cruzada commit→issue queda permanente |
| 2026-05-11 | Parseo | Tres referencias hardcoded a `"v5.15"` en `main.py` mientras `config/settings.py:VERSION="5.20"`. `--version` mostraba v5.20 (ya usaba la constante) pero `--help` y banners mostraban v5.15. Bug arrastrado desde bump v5.19→v5.20 (commit `cceccd1`) sin actualizar todos los sitios | REGLA canónica formalizada en SPEC v4.11: en Parseo la constante `VERSION` vive ÚNICAMENTE en `config/settings.py`. Cualquier referencia en código de aplicación se hace por f-string sobre la constante (`f"v{VERSION}"`). Prohibido literalizar el número en `main.py`, banners o argparse description. Bumpear versión = modificar UNA sola línea |
| 2026-05-12 | Parseo/extractores/panruje | Descuadre 68,64€ en FT A26 58 (22/04/2026): el extractor leía el cuadro fiscal del footer como fuente única para desglose IVA, pero la plantilla 2T26 con columnas R.Equ. (sub-variante C2) puede renderizar el footer con UNA SOLA fila de IVA en lugar de las DOS reales — pierde la fila 4% y solo desglosa el 21%. TOTAL final del PDF correcto pero el extractor cuadra con base/iva incompleto | REGLA canónica formalizada en SPEC v4.12: para facturas con potencial IVA mixto, el extractor debe parsear las líneas del **body** (donde IVA por línea es explícito y robusto) y agrupar por tasa para reconstruir totales. Footer es solo cross-check, no fuente única. Aplicado en `panruje.py` con regex `RE_LINEA_BODY_CON_IVA`. Fallback al método legacy preserva Patrón A puro. Tests con 4 PDFs reales cubriendo 3 patrones evolutivos del proveedor son fundamentales — sin fixtures reales no detectas sub-variantes (C1 footer OK vs C2 footer roto se diferencian solo en datos reales) |
| 2026-05-12 | Parseo/main.py orquestador | El orquestador downstream consolida las N líneas del extractor en 1 sola fila en hoja `Lineas` con IVA único y BASE=TOTAL/(1+IVA/100). Para PANRUJE multi-IVA, BASE escrita ya no coincide con suma real (89,62€ vs 86,30€ en FT-A26-58) | REGLA canónica formalizada en SPEC v4.12 decisión #5: una línea por factura en COMPRAS, NO por tasa de IVA. Razón: el Excel COMPRAS se usa para conocer el coste, NO para llevar el registro fiscal de IVA. BASE escrita es algebraicamente coherente con la fila (BASE × IVA = TOTAL) aunque no refleja la mezcla real. Aceptable porque BASE de COMPRAS no alimenta declaraciones fiscales — Kinema/Modelo 303 van aparte. NO romper esta convención para "ganar precisión fiscal en COMPRAS" — consumidores downstream (Streamlit, dashboards) asumen base × (1+iva) = total |
| 2026-05-13 | git/github | `closes #N` se reprodujo en cluster ECOMS (PR #13). Esta vez el auto-cierre del issue #12 fue correcto (issue del cluster = scope del PR) pero al postear comentario con `gh issue close 12 --comment "..."` el comando rebotó con "already closed" y NO posteó el comentario | REGLA refinada: tras merge con `Closes #N` automático, postear comentario aclaratorio SIEMPRE como paso SEPARADO con `gh issue comment N --body "..."`. Nunca usar `gh issue close N --comment "..."` después de un merge — el flag `--comment` del close se pierde si GitHub ya cerró el issue antes |
| 2026-05-13 | Parseo/extractores/ecoms saneamiento | Combinando saneamiento OCR regex con diccionario de correcciones por keywords, el orden importa. Mi intuición inicial fue "correcciones primero para preservar el texto original" — incorrecta. La realidad: el saneamiento desbloquea matches al normalizar separadores (`0 7182` → `0,7182` hace que la keyword `7182` sea localizable DENTRO del decimal). Y las keywords del diccionario son substring (`in`), no exactas, así que sobreviven al saneamiento sin problema | REGLA: cuando combines normalización regex + diccionario de keywords, aplicar normalización PRIMERO. Las keywords substring sobreviven al saneamiento; el saneamiento desbloquea casos donde el OCR rompe estructura (separador decimal, signos de IVA, etc.) y sin él las regex de localización (p.ej. "encontrar primer decimal de 4-5 cifras") fallan |
| 2026-05-13 | CI/tests | Cluster ECOMS pasó local con 17/17 pero CI rojo con 12 tests failed: `tesseract`/`poppler` no instalados en runner. El extractor define `OCR_DISPONIBLE = try/except ImportError` y `_extraer_texto_ocr` retorna `""` silenciosamente si no hay OCR runtime. Local pasaba porque la PC del usuario tiene tesseract instalado | REGLA: cualquier test que dependa de runtime externo (OCR, fuentes del sistema, sockets, GPU, binarios opcionales) DEBE llevar `@pytest.mark.skipif` condicional sobre la disponibilidad del runtime. Patrón: en el módulo testeado exportar `RUNTIME_DISPONIBLE = bool`, en el test `pytest.mark.skipif(not RUNTIME_DISPONIBLE, reason="...")`. NO asumir paridad de entornos dev/CI |
| 2026-05-13 | Drive↔repo drift | `DiccionarioProveedoresCategoria.xlsx` tocado desde código en 2 sesiones consecutivas (12/05 y 13/05). El Drive no es git: si alguien edita el Excel mañana, no hay forma de saber qué cambió ni revertir más allá de los backups locales puntuales | REGLA: cualquier dato compartido en Drive/Cloud que se mute desde código necesita protocolo de versionado. Mínimo: snapshot diario al repo (`gestion-facturas/datos/backups/<archivo>_<fecha>.xlsx`). Detección de drift: hash de la última versión registrada vs actual, alerta si difieren entre ejecuciones. La regla "lectura ≠ escritura: paths divergentes" (06/05/2026) cubría el caso de fuentes desincronizadas; esto es el complemento: una sola fuente pero sin trazabilidad de mutaciones |
| 2026-05-13 | metodología | Tests unitarios verdes ≠ bug reproducido en producción. El commit 1 del cluster ECOMS (`fix(ecoms): leer todas las páginas`) describía un bug que los tests parametrizados aislados validaban (TOTAL None en formato canje), pero el dry-run 2T26 pre/post NO mostraba diferencia (DIA 2T26_0420 TOTAL=4,68€ idéntico antes y después). Existe ruta secundaria en `main.py` que ya leía multi-página en ciertos casos. El fix sigue siendo correcto, pero el alcance real es menor del descrito en el mensaje del commit | REGLA: SIEMPRE comparar dry-run pre/post como prueba de impacto real, no solo tests aislados, ANTES de afirmar magnitud del bug en commit messages, reports o cierre. El gap entre "tests verdes" y "comportamiento en producción" puede ser grande cuando el sistema tiene múltiples rutas (orquestador con fallbacks). Protocolo: para todo fix con commit message que afirme severidad ("CRÍTICO", "data loss", "siempre None"), validar empíricamente el delta con dry-run antes del merge. Si el dry-run muestra cero impacto, ajustar el wording del commit a la severidad real ("regresión hipotética cuando X", no "siempre Y") |
| 2026-05-13 | git/github (3ª manifestación) | `Closes #N` literal en commit body disparó auto-cierre **aunque iba entre paréntesis explicando que se omitía deliberadamente**. Commit `cc4714b` del cluster BM contenía `"(Closes #14 deliberadamente OMITIDO — leccion v4.13 #1...)"` para preservar control manual del cierre, pero GitHub parsea con regex sin entender contexto sintáctico ni semántico → vio `Closes #14` y cerró el issue al merge | REGLA refinada: NUNCA escribir la cadena literal `Closes #N` / `Fixes #N` / `Resolves #N` en ningún commit message del PR, **ni siquiera negada, comentada, entre paréntesis o explicando que se omite**. Si hay que mencionar el comportamiento de auto-cierre como nota, hacerlo fuera del commit body (PR body o comentario tras merge). Usar siempre `Refs #N` / `Tracks #N` / `Related to #N` |
| 2026-05-13 | metodología (refuerzo cuantificado v4.13 #5) | Segunda manifestación del gap test_aislado ≠ producción, con datos numéricos concretos para fixture 1215 (Colgate TOTAL + Oferta) sobre baseline main pre-fix: `extraer_lineas()` aislado devuelve SUMA=0.84€ con Δ=+6.05€ (parseo infravalora); `main.py` dry-run completo devuelve Total=9.99€ con Δ=−3.10€ (parseo sobrevalora). **Mismo PDF, mismo código, signo opuesto.** Tras fix convergen a 6.89€ Δ=0. Causa: wrapper `fallback_sintetico` en `main.py` que inventa líneas cuando `extraer_lineas` devuelve algo anómalo, a veces sobrecompensa | REGLA: dry-run pre/post **NO opcional** para PRs de extractor. Los tests aislados NO sustituyen — pueden dar números distintos en **magnitud y signo** para el mismo PDF. El número del test crudo es solo un eslabón; el número de producción es el del orquestador. Cuando un test aislado y el dry-run de producción den números muy distintos, investigar el wrapper de fallback antes de declarar el bug resuelto |
| 2026-05-13 | Parseo/extractores/bm — dominio retail | Tickets de supermercado mezclan dos tipos de descuento con semánticas opuestas: `Oferta -X.XX` es informativa (el IMPORTE de la línea de producto YA está descontado en el ticket — el cliente paga ese importe ya rebajado); `Promoción ...` y `Vales ...` son descuentos reales (el IMPORTE de la línea es PVP y el descuento aún no se aplicó). En BM ambas líneas son negativas. Un regex genérico `^(.+?)\s+(-[\d.,]+)$` las captura por igual y un post-procesado que suma negativos las aplica todas → doble resta sobre `Oferta` | REGLA: el parser de descuentos retail debe distinguir por **keyword distintiva** (`Oferta` vs `Promoción`/`Vales`), no por signo del importe. Antes de decidir el regex, validar con un ticket que tenga AHORRO claro: el TOTAL del ticket impreso ya incluye las Ofertas restadas (verificar `TICKET SIN AHORROS / AHORRO`). Caso real BM cluster (PR #15): 4 fixtures con Oferta dispararon Δ entre +0,27€ y +1,66€ por doble resta consistente |
| 2026-05-13 | Parseo/extractores/bm — skip_patterns | `r'\bTOTAL\b'` como skip_pattern, pensado para descartar `TOTAL COMPRA (iva incl.) X.XX`, también cazaba cualquier producto cuyo nombre contenía "TOTAL" (Colgate Total, Listerine Total, P.Dental Total Prev). El producto se perdía entero upstream, y el descuento posterior (Promoción/Vales) quedaba absorbido por la línea anterior incorrecta en `lineas_filtradas[-1]`, **enmascarando el bug como si fuera de otra naturaleza** (parecía Oferta doble-restada, era línea perdida) | REGLA: `skip_patterns` con `\b` sin anclaje son frágiles cuando el token es genérico (TOTAL, FACTURA, producto, género, oferta con...). Anclar a inicio de línea (`^\s*TOKEN\b`) o a contexto específico (`\bTOTAL COMPRA\b`) cuando la palabra pueda aparecer en descripciones de producto del catálogo. Decisión canónica #7 (SPEC v4.14): auditoría barrida obligatoria de skip_patterns con `\b` genéricos contra el catálogo del proveedor antes de release de cualquier extractor retail |
| 2026-05-14 | metodología / docs maestros | Errata v4.14: el SPEC atribuía el descuadre del fixture 1215 a `fallback_sintetico` en `main.py` (visible en `tests/test_generico.py`). El mecanismo real es `_merge_resultados` en `main.py:1582-1669` (merge primario↔fallback OCR). Confundí dos mecanismos con nombres similares — `_linea_sintetica_desde_total` en `generico.py` (que SÍ se llama "fallback sintético" en el código) vs el merge OCR en main.py — sin verificarlo empíricamente. La afirmación parecía técnica precisa pero era hipótesis no verificada que terminó siendo incorrecta | REGLA: en SPEC, CHANGELOG y documentos maestros, cada afirmación de mecanismo concreto debe ser empíricamente reproducible o etiquetarse explícitamente como hipótesis pendiente. Cuando atribuyas un comportamiento a una función específica, citar archivo + rango de líneas + haber leído ese rango. Si solo es hipótesis, escribir "hipótesis pendiente de verificar" — NO redactar como observación. La diferencia entre "hay un wrapper en main.py" (hipótesis incorrecta) y "el mecanismo real es `_merge_resultados` en `main.py:1582-1669`" (observación verificada con grep+lectura) no es matiz menor — son reproducibilidades cualitativamente distintas. Protocolo de errata: si se detecta una atribución incorrecta antes del próximo bump, añadir bloque "Errata YYYY-MM-DD" al final del CHANGELOG correspondiente sin bumpear; formalizar la corrección en el siguiente bump |
| 2026-05-14 | metodología | Una sesión de investigación cuya hipótesis original queda invalidada empíricamente también es valor. Auditoría merge sobre 627 facturas: 0,16% activación, 0 sospechosas → la hipótesis "merge enmascara bugs sistemáticamente" (TBD reformulado v4.14) queda invalidada. NO es "sesión sin resultado" — es resultado positivo: reduce el espacio de causas posibles para descuadres futuros y orienta el trabajo a las causas reales (skip_patterns) | REGLA: documentar hipótesis invalidadas en CHANGELOG con los datos que las invalidan, no enterrarlas. Sirven para (a) prevenir reabrir la misma investigación cuando alguien re-descubra el TBD, (b) redirigir el esfuerzo de futuras sesiones a causas reales, (c) registrar evidencia empírica acumulada del comportamiento del sistema. Si tras una auditoría no hay bugs, escribirlo explícitamente con métricas. La ausencia de hallazgo es información si se documenta |
| 2026-05-14 | metodología / observabilidad | Patrón "sub-validación contra baseline pre-fix": cuando una auditoría se ejecuta sobre código post-fix (típico cuando el bug que motivó la sesión ya está resuelto), validar la instrumentación contra el código pre-fix sobre los casos conocidos. En esta sesión: rollback selectivo de `bm.py`+`settings.py` a `517cf1f` (manteniendo `main.py` con instrumentación, posible porque `main.py` no había cambiado entre 517cf1f y HEAD) + dry-run 1T26 → caso 1215 reapareció con perfil C3+M2 swing −3,86€ → la instrumentación habría capturado el patrón histórico | REGLA: inversión 5-10 minutos, blindaje sustancial. Convierte "el código actual funciona bien" en "el código actual funciona bien Y el código antiguo se caracterizaba correctamente por la instrumentación". Aplicable a cualquier sesión de observabilidad o refactor con casos históricos conocidos. Mecánica: identificar el commit que precedió al fix (`git log --oneline`), hacer `git checkout <sha> -- archivo_clave` (sin checkout completo, evita conflictos con instrumentación), ejecutar reproducción mínima, restaurar con `git checkout HEAD -- archivo_clave`. Documentar el comando exacto en commit body para reproducibilidad |
| 2026-05-14 | metodología / auditoría | Validación cuantitativa de hipótesis de impacto. La auditoría canónica #7 (SPEC v4.14) nació de una predicción razonable: si BM tenía el problema, otros extractores BM-like probablemente también. El dato empírico (1/116 = 0,86%) invalidó la predicción de impacto sistemático. Refuerza lección v4.15 #2: hipótesis razonables pueden necesitar invalidación empírica, no solo extrapolación lógica | REGLA: cuando una sesión cierra un cluster con N items afectados, antes de extrapolar a "muchos más casos" en otros extractores, auditar barriendo es la inversión correcta. Esta sesión: ~15 min reales para cubrir 116 extractores con cross-match contra diccionario, cierra el debate Y produce el fix prioritario en el mismo flujo. Mecánica: script auxiliar `_*.py` (gitignored por convención) que parsea AST/regex de los extractores, carga catálogo, hace cross-match, ordena por severidad. Tras la auditoría: 1 fix puntual al hallazgo ALTA, resto como TBDs nominales documentados |
| 2026-05-14 | metodología / tests | Patrón "fix durante test reveals bug adyacente". Al construir el fixture sintético para `tests/extractores/test_la_cuchara.py` se reveló que el regex de captura `[A-Z0-9\s%]` del extractor LA CUCHARA NO permite puntos. Pero los productos del catálogo tienen punto (`SSA VALENTINA E.AMARILLA 370ML`). Bug adyacente no relacionado al fix de VALE, descubierto al forzar input válido para la regex | REGLA: documentar como TBD cualquier hallazgo de este tipo, aunque sea ortogonal al fix en curso. NO ampliar el alcance del PR actual — bug adyacente va a sesión propia. El test debe pasar con input compatible con la implementación actual; el TBD captura la limitación descubierta. Valor del trabajo de tests: forzar sintetizar input válido revela suposiciones implícitas del extractor que no se manifestaban en el camino feliz |
| 2026-05-14 | python / estilo | `set` vs `list` para filtros: tipar la intención. Fix LA CUCHARA usó `ignorar_exacto = {'TOTAL','VALE'}` (set) y `ignorar_substring = ['IVA 10']` (list). El tipo de colección expresa la semántica de búsqueda: set para exact-match (O(1) y semántica clara — `desc in set` es exact), list para iteración con `in` substring (orden potencialmente relevante — `any(x in desc for x in list)`). Mejor que dos listas con comentarios explicando cuál es cuál | REGLA: para filtros con semánticas mixtas (parte exact-match, parte substring), usar tipos de colección distintos como documentación auto-explicativa. El tipo de la variable es contrato: set significa "lookup exacto y rápido", list significa "iterar y comparar". También vale tuple si la lista es inmutable. Aplicar a cualquier refactor de extractores con filtros y al cluster B item 3/3 JIMELUZ |
| 2026-05-14 | Parseo / decisión canónica #8 | Exact-match por defecto en filtros de cabecera (formalizado en SPEC v4.16 como decisión canónica #8). Las cabeceras fiscales (TOTAL, VALE, BASE, IVA, CIF, NIF, FECHA) aparecen como palabras aisladas en líneas que NO son productos. Los nombres de productos pueden contener esas palabras como substring (VALENTINA contiene VALE, Colgate Total contiene TOTAL). Fix LA CUCHARA es el caso paradigmático: 4 productos VALENTINA del catálogo se descartaban silenciosamente porque `'VALE' in 'VALENTINA'` = True | REGLA: en filtros de "ignorar" para extractores, exact-match es default. Substring solo cuando el contexto fuerza inequívocamente unicidad (ej. `IVA 10` siempre va con porcentaje pegado: `IVA 10%(950,00): 95,00`, nunca como descripción de producto). Implementación recomendada: `set` para exact, `list/tuple` para substring (ver lección "set vs list para filtros"). Aplica a TODOS los extractores con filtros de keywords — auditar antes de tocar lógica de extracción nueva |
| 2026-05-15 | metodología / auditoría (4ª lección eje "no convertir datos en trabajo") | Filtro de importancia operacional. Tras la auditoría overnight del wrapper `_linea_sintetica_desde_total` (26 activaciones detectadas → 10 sospechosas mecánicas → 2 accionables tras triage humano), formalizado en SPEC v4.17. Sin un 2º filtro de importancia operacional, las auditorías de barrido inflan el backlog con casos que nunca volverán a aparecer (proveedores no recurrentes, importes inmateriales, falsos positivos del threshold del clasificador). Ilustración cuantitativa: clasificador mecánico produjo 10 sospechosos. Filtro de importancia humano dejó 2. Sin filtro, ~8 TBDs nuevos sobre proveedores irrelevantes habrían inflado el backlog | REGLA: cada hallazgo de una auditoría debe pasar DOS filtros antes de generar TBD: (1) significancia estadística (validación cuantitativa, lecciones v4.15-v4.16), (2) relevancia operacional (proveedor recurrente o importe material). Protocolo: tras clasificar mecánicamente, el responsable del negocio (Jaime, NO Claude) marca cada cluster como ACCIONABLE / ARCHIVAR. Solo ACCIONABLE se eleva a TBD del SPEC. Los ARCHIVAR se mencionan explícitamente en el CHANGELOG con su razón ("no recurrente" / "importe inmaterial") para evitar que la próxima auditoría los redescubra. Si en futuras auditorías un archivado reaparece con frecuencia creciente, reabrir entonces |
| 2026-05-14 | Parseo/extractores | CIFs hardcodeados en extractores pueden ser fantasma. En `cafes_pozo.py` el atributo de clase `cif = 'A28136189'` venía con comentario "(del registro mercantil)" — pista falsa: el CIF real `A28917250` está en el footer `ESA28917250` de cada factura. Verificación empírica sobre 3 PDFs (1T26/2T26) confirmó la divergencia. Impacto: falso positivo `DISCREPANCIA_NOMBRE_CIF` por cada factura del proveedor mientras esté activado el cruce nombre+CIF de la skill `gestion-facturas` §147 | REGLA: al auditar/crear extractor con `cif` declarado: (1) extraer CIF empíricamente del footer del PDF con regex `ES?(A\d{8})`; (2) comparar con `MAESTRO_PROVEEDORES.xlsx`; (3) si MAESTRO y PDF coinciden pero el código difiere → el código está mal. Detección preventiva (TBD): test que verifique `extractor.cif == cif_extraído_de_PDF_de_muestra` para cada extractor con `cif` declarado; vale la pena tooling automático o queda como ítem manual del checklist. Fix de referencia: Parseo commit `312afe6`. Issue relacionada gmail.py renombrado por sender: Parseo#20 |
